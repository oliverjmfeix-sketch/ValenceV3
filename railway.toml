# Railway Configuration for Valence Backend

[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c './scripts/auto_reseed.sh && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/health"
healthcheckTimeout = 300
